--- a/build/make/core/rbe.mk
+++ b/build/make/core/rbe.mk
@@ -19,7 +19,7 @@
   ifdef RBE_DIR
     rbe_dir := $(RBE_DIR)
   else
-    rbe_dir := prebuilts/remoteexecution-client/live/
+    rbe_dir := prebuilts/remoteexecution-client/buildbuddyfix/
   endif
 
   ifdef RBE_CXX_POOL
@@ -68,7 +68,9 @@
   cxx_platform := $(platform),Pool=$(cxx_pool)
   java_r8_d8_platform := $(platform),Pool=$(java_pool)
 
-  RBE_WRAPPER := $(rbe_dir)/rewrapper
+  # Some remote executors reject actions with working_directory=".".
+  # Canonicalizing the working dir helps once the client itself is patched.
+  RBE_WRAPPER := $(rbe_dir)/rewrapper -canonicalize_working_dir
   RBE_CXX := --labels=type=compile,lang=cpp,compiler=clang --env_var_allowlist=PWD --exec_strategy=$(cxx_rbe_exec_strategy) --platform=$(cxx_platform) --compare=$(cxx_compare)
 
   # Append rewrapper to existing *_WRAPPER variables so it's possible to
--- a/build/soong/remoteexec/remoteexec.go
+++ b/build/soong/remoteexec/remoteexec.go
@@ -33,7 +33,7 @@
 	DefaultImage = "docker://gcr.io/androidbuild-re-dockerimage/android-build-remoteexec-image@sha256:1eb7f64b9e17102b970bd7a1af7daaebdb01c3fb777715899ef462d6c6d01a45"
 
 	// DefaultWrapperPath is the default path to the remote execution wrapper.
-	DefaultWrapperPath = "prebuilts/remoteexecution-client/live/rewrapper"
+	DefaultWrapperPath = "prebuilts/remoteexecution-client/buildbuddyfix/rewrapper"
 
 	// DefaultPool is the name of the pool to use for remote execution when none is specified.
 	DefaultPool = "default"
--- a/build/soong/remoteexec/remoteexec_test.go
+++ b/build/soong/remoteexec/remoteexec_test.go
@@ -74,7 +74,7 @@
 			PoolKey:           "default",
 		},
 	}
-	want := fmt.Sprintf("prebuilts/remoteexecution-client/live/rewrapper --labels=compiler=clang,lang=cpp,type=compile --platform=\"Pool=default,container-image=%s\" --exec_strategy=local --inputs=$in --output_files=$out --env_var_allowlist=LANG,LC_MESSAGES,PYTHONDONTWRITEBYTECODE -- ", DefaultImage)
+	want := fmt.Sprintf("prebuilts/remoteexecution-client/buildbuddyfix/rewrapper --labels=compiler=clang,lang=cpp,type=compile --platform=\"Pool=default,container-image=%s\" --exec_strategy=local --inputs=$in --output_files=$out --env_var_allowlist=LANG,LC_MESSAGES,PYTHONDONTWRITEBYTECODE -- ", DefaultImage)
 	if got := params.NoVarTemplate(DefaultWrapperPath); got != want {
 		t.Errorf("NoVarTemplate() returned\n%s\nwant\n%s", got, want)
 	}
--- a/build/soong/android/config.go
+++ b/build/soong/android/config.go
@@ -2400,7 +2400,7 @@
 }
 
 func (c *config) RBEWrapper() string {
-	return c.GetenvWithDefault("RBE_WRAPPER", remoteexec.DefaultWrapperPath)
+	return c.GetenvWithDefault("RBE_WRAPPER", remoteexec.DefaultWrapperPath+" -canonicalize_working_dir")
 }
 
 // UseHostMusl returns true if the host target has been configured to build against musl libc.
--- a/build/soong/ui/build/config.go
+++ b/build/soong/ui/build/config.go
@@ -1596,7 +1596,7 @@
 	if v, ok := c.environ.Get("RBE_DIR"); ok {
 		return v
 	}
-	return "prebuilts/remoteexecution-client/live/"
+	return "prebuilts/remoteexecution-client/buildbuddyfix/"
 }
 
 func (c *configImpl) rbeReproxy() string {
--- a/build/make/rbesetup.sh
+++ b/build/make/rbesetup.sh
@@ -33,7 +33,7 @@
 # This function prefixes the given command with appropriate variables needed
 # for the build to be executed with RBE.
 function use_rbe() {
-  local RBE_BINARIES_DIR="prebuilts/remoteexecution-client/latest"
+  local RBE_BINARIES_DIR="prebuilts/remoteexecution-client/buildbuddyfix"
   local DOCKER_IMAGE="gcr.io/androidbuild-re-dockerimage/android-build-remoteexec-image@sha256:1eb7f64b9e17102b970bd7a1af7daaebdb01c3fb777715899ef462d6c6d01a45"
 
   # Do not set an invocation-ID and let reproxy auto-generate one.
--- a/build/soong/docs/rbe.json
+++ b/build/soong/docs/rbe.json
@@ -13,7 +13,7 @@
         "RBE_instance": "[replace with your RBE instance]",
         "RBE_service": "[replace with your RBE service endpoint]",
 
-        "RBE_DIR": "prebuilts/remoteexecution-client/live",
+        "RBE_DIR": "prebuilts/remoteexecution-client/buildbuddyfix",
 
         "RBE_use_application_default_credentials": "true",
